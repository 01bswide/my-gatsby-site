import * as React from "react";
import Layout from "../components/layout";

const IndexPage = () => {
  return (
    <Layout pageTitle="Home Page">
      <p>This is the home page from the gatsby tutorial.</p>
    </Layout>
  );
};

export default IndexPage;
